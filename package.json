{
  "name": "remember-when-monorepo",
  "version": "1.1.4",
  "description": "Orchestration for Remember When project",
  "private": true,
  "scripts": {
    "test": "cd remember-when-cli && npm test",
    "deploy": "./deploy.sh"
  }
}
